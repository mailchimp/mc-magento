dist: trusty
language: php
php:
  #- 5.4
  #- 5.5
  #- 5.6
  - 7.0

before_script:
  - sleep 30

script:
  - cd ..
  - wget https://github.com/magento/marketplace-eqp/archive/1.0.5.zip
  - unzip -qq 1.0.5.zip
  - wget https://github.com/squizlabs/PHP_CodeSniffer/releases/download/2.6.2/phpcs.phar && chmod +x phpcs.phar
  - pwd
  - ls -la
  - php phpcs.phar --config-set installed_paths ./marketplace-eqp-1.0.5/
  - php phpcs.phar -n --standard="MEQP1" ./mc-magento/

#notifications:
#  slack: ebizmarts:DqCqxnqWzW55zoFU069hxs1T